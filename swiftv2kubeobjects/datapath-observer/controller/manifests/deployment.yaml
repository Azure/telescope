apiVersion: apps/v1
kind: Deployment
metadata:
  name: datapath-controller
  namespace: perf-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: datapath-controller
  template:
    metadata:
      labels:
        app: datapath-controller
    spec:
      serviceAccountName: datapath-controller
      containers:
      - name: controller
        image: acndev.azurecr.io/datapath-controller:2026.01.02.01
        args:
        - "--namespace=slo-1"
        - "--label-selector=podgroup=deployment-churn"
        - "--max-concurrent-reconciles=100"
        ports:
        - name: http
          containerPort: 8080
