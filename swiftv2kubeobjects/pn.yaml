apiVersion: multitenancy.acn.azure.com/v1alpha1
kind: PodNetwork
metadata:
   name: pn100
   finalizers: 
   - finalizers.acn.azure.com/dnc-operations
spec:
   networkID: ${custVnetGUID}
   subnetResourceID: ${custSubnetResourceId}
   subnetGUID: ${custSubnetGUID}
   deviceType: acn.azure.com/vnet-nic
