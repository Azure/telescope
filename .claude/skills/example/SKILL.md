---
name: example
description: Generate example pipeline YAML from KCL source
---

# Generate example pipeline YAML file

In the `kcl/example_pipeline` folder, run the following command.
```bash
kcl run pipeline.k -S output -o pipeline.yaml
```
