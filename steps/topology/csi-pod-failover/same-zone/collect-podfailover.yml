parameters:
- name: cloud
  type: string
  default: ''
- name: regions
  type: object
- name: engine_input
  type: object

steps:
- template: /steps/engine/podfailover/samezone/collect.yml
  parameters:
    cloud: ${{ parameters.cloud }}
    result_dir: $(System.DefaultWorkingDirectory)/$(RUN_ID)
    test_times: ${{ parameters.engine_input.test_times }}
