parameters:
- name: cloud
  type: string
- name: engine
  type: string
- name: regions
  type: object

steps:
- template: /steps/cloud/${{ parameters.cloud }}/update-kubeconfig.yml
  parameters:
    role: nap
    region: ${{ parameters.regions[0] }}

- ${{ if eq(variables['JUMPBOX_HOST'], '') }}:
  # Public cluster: run locally on agent
  - bash: |
      set -euo pipefail
      set -x
      KARPENTER_NODEPOOL_FILE=$(pwd)/scenarios/$(SCENARIO_TYPE)/$(SCENARIO_NAME)/kubernetes/karpenter_nodepool.${{ parameters.cloud }}.yml
      
      kubectl apply -f $KARPENTER_NODEPOOL_FILE

      # Patch the On-Demand NodePool
      kubectl patch nodepool default --type='json' -p="[{'op': 'replace', 'path': '/spec/template/spec/requirements/2/values', 'value': ['$VM_SIZE']}]"

      # Patch the Spot NodePool
      kubectl patch nodepool spot --type='json' -p="[{'op': 'replace', 'path': '/spec/template/spec/requirements/2/values', 'value': ['$VM_SIZE']}]"

      kubectl get nodepool default -o yaml
      kubectl get nodepool spot -o yaml
    env:
      VM_SIZE: ${{ parameters.vm_size }}
    displayName: "Validate Karpenter setup (local)"

- ${{ if ne(variables['JUMPBOX_HOST'], '') }}:
  # Private cluster: run on jumpbox
  - template: /steps/cloud/${{ parameters.cloud }}/run-on-jumpbox.yml
    parameters:
      displayName: "Validate Karpenter setup"
      script: |
        set -euo pipefail
        set -x
        KARPENTER_NODEPOOL_FILE=$(pwd)/scenarios/$(SCENARIO_TYPE)/$(SCENARIO_NAME)/kubernetes/karpenter_nodepool.${{ parameters.cloud }}.yml
        
        kubectl apply -f $KARPENTER_NODEPOOL_FILE

        # Patch the On-Demand NodePool
        kubectl patch nodepool default --type='json' -p="[{'op': 'replace', 'path': '/spec/template/spec/requirements/2/values', 'value': ['$VM_SIZE']}]"

        # Patch the Spot NodePool
        kubectl patch nodepool spot --type='json' -p="[{'op': 'replace', 'path': '/spec/template/spec/requirements/2/values', 'value': ['$VM_SIZE']}]"

        kubectl get nodepool default -o yaml
        kubectl get nodepool spot -o yaml
