parameters:
- name: cloud
  type: string
  default: ''
- name: engine_input
  type: object
  default: {}
- name: regions
  type: object
  default: {}
- name: topology
  type: string
- name: node_count
  type: number
- name: max_pods
  type: number
- name: load_type
  type: string
- name: operation_timeout
  type: string

steps:
- template: /steps/engine/clusterloader2/cri-eviction-eval/execute.yml
  parameters:
    cloud: ${{ parameters.cloud }}
    engine_input: ${{ parameters.engine_input }}
    region: ${{ parameters.regions[0] }}
    topology: ${{ parameters.topology }}
    node_count: ${{ parameters.node_count }}
    max_pods: ${{ parameters.max_pods }}
    load_type: ${{ parameters.load_type }}
    operation_timeout: ${{ parameters.operation_timeout }}
