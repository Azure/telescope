# steps/topology/k8s-cluster-crud-machine/collect-machine.yml
parameters:
  - name: cloud
    type: string
  - name: regions
    type: object
  - name: engine_input
    type: object
    default: {}

steps:
  - template: /steps/engine/machine/k8s/collect.yml
    parameters:
      cloud: ${{ parameters.cloud }}
      region: ${{ parameters.regions[0] }}
