apiVersion: v1
kind: Service
metadata:
  name: opencost-service
  namespace: kube-system
spec:
  selector:
    app: cost-analysis-agent
  ports:
    - name: opencost-service
      protocol: TCP
      port: 9003
      targetPort: 9003
  type: ClusterIP
