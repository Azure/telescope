trigger: none
schedules:
  # On-Demand Schedule with Morgan Stanley cluster config
  - cron: "0 */3 * * *"
    displayName: "Every 3 hours"
    branches:
      include:
        - ms-nap-complex
    always: true
variables:
  SCENARIO_TYPE: perf-eval
  SCENARIO_NAME: nap

stages:
  - stage: azure_eastus2_ondemand_complex_config
    dependsOn: []
    variables:
      - group: NAP-5K-Config
    jobs:
      - template: /jobs/competitive-test.yml
        parameters:
          cloud: azure
          regions:
            - eastus2
          terraform_input_file_mapping:
            - eastus2: "scenarios/perf-eval/nap/terraform-inputs/azure-complex.tfvars"
          engine: clusterloader2
          engine_input:
            image: "ghcr.io/azure/clusterloader2:v20250423"
          topology: karpenter
          matrix:
            complex-nap: # 16 cores cpu per pod with 64Gi memory
              node_count: 2 # let karpenter scale to provision 5000 pods
              pod_count: 10
              scale_up_timeout: "200m"
              scale_down_timeout: "200m"
              node_label_selector: "karpenter.sh/nodepool = default"
              node_selector: "{karpenter.sh/nodepool: default}"
              loop_count: 1
              warmup_deployment: true
              warmup_deployment_template: warmup_deployment.yaml
              capacity_type: on-demand
              aad_enabled: "true"
              vm_size: "D"
              enable_apiserver_vnet_integration: "false"
          max_parallel: 1
          timeout_in_minutes: 60
          credential_type: service_connection
          ssh_key_enabled: true
