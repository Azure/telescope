trigger: none

variables:
  SCENARIO_TYPE: perf-eval
  SCENARIO_NAME: job-scheduling
  SCENARIO_VERSION: main

jobs:
- template: /jobs/competitive-test.yml
  parameters:
    cloud: azure
    regions:
      - eastus2
    engine: clusterloader2
    
    engine_input:
      image: "ghcr.io/azure/clusterloader2:v20241022"
      config_dir: "$(Pipeline.Workspace)/s/scenarios/job-scheduling/cl2-config"
      config_file: "config.yml"
      report_dir: "$(Pipeline.Workspace)/s/scenarios/job-scheduling/results"
    topology: kwok
    matrix:
      default:
        
        kwok_nodes: 20
        cpu_per_node: 4
        node_count: 20
        node_per_step: 1
        max_pods: 100
        repeats: 1
        operation_timeout: "15m"
        cilium_enabled: False
        service_test: False
        cnp_test: False
        ccnp_test: False
        num_cnps: 0
        num_ccnps: 0
        dualstack: False
      
    max_parallel: 1
    timeout_in_minutes: 360
    credential_type: service_connection
    ssh_key_enabled: false