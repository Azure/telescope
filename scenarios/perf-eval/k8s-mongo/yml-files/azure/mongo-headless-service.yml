apiVersion: v1
kind: Service
metadata:
  name: mongo-server
  labels:
    app: mongo-server
spec:
  ports:
  - name: mongo-server
    port: 27017
    targetPort: 27017
  clusterIP: None
  selector:
    app: mongo-server