apiVersion: v1
kind: Pod
metadata:
  name: pod3
  namespace: default
  labels:
    app: test
  # annotations:
  #   k8s.v1.cni.cncf.io/networks: ipv4-l3-node1
spec:
  nodeSelector:
    # kubernetes.io/hostname: aks-user-40702790-vms1
    kubernetes.io/hostname: aks-default-11967610-vmss000001
  containers:
    - name: tester
      image: busybox:latest
      command: ["sleep", "infinity"]
---
apiVersion: v1
kind: Pod
metadata:
  name: pod4
  namespace: default
  labels:
    app: test
  # annotations:
  #   k8s.v1.cni.cncf.io/networks: ipv4-l3-node2
spec:
  nodeSelector:
    # kubernetes.io/hostname: aks-user-40702790-vms2
    kubernetes.io/hostname: aks-default-11967610-vmss000002
  containers:
    - name: tester
      image: busybox:latest
      command: ["sleep", "infinity"]
