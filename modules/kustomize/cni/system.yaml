apiVersion: v1
kind: Pod
metadata:
  name: bridge-pod
  namespace: default
  labels:
    app: bridge
spec:
  nodeSelector:
    kubernetes.io/hostname: aks-default-11967610-vmss000000
  containers:
    - name: tester
      image: busybox:latest
      command: ["sleep", "infinity"]

