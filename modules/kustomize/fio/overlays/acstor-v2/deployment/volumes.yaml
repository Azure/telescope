apiVersion: batch/v1
kind: Job
metadata:
  name: test
spec:
  template:
    spec:
      containers:
        - name: fio
          volumeMounts:
            - name: acstor-mount
              mountPath: /mnt/data
      volumes:
        - name: acstor-mount
          persistentVolumeClaim:
            claimName: acstor-mount
