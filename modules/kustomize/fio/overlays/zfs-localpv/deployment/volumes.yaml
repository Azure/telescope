apiVersion: batch/v1
kind: Job
metadata:
  name: $name
spec:
  template:
    spec:
      containers:
        - name: fio
          volumeMounts:
            - name: zfs-mount
              mountPath: /mnt/data
      volumes:
        - name: zfs-mount
          persistentVolumeClaim:
            claimName: $name
