apiVersion: batch/v1
kind: Job
metadata:
  name: {{.Name}}
  labels:
    group: {{.Group}}
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - image: busybox
        name: {{.Name}}
        command: ["sh", "-c", "echo Job is running; sleep 5; echo Job done!"]

     

