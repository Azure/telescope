apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: {{.basename}}
spec:
  endpointSelector:
    matchLabels:
      app: nginx
  egress:
  - toEndpoints:
    - {}
  ingress:
  - fromEndpoints:
    - {}