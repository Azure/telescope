apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: {{.basename}}-{{.namespace}}
  namespace: {{.namespace}}
spec:
  endpointSelector:
    matchLabels:
      app: nginx
      actualLabelName: val
  egress:
  - toEndpoints:
    - {}